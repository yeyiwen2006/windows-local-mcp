# 验证记录

[English](VALIDATION.en.md) · **简体中文**

本文件记录 ChatGPT Windows Local MCP 的主要验证结果。它不是安全认证报告，也不代表所有 Windows 环境都已覆盖。

## 0.1.3：桌面目标锁

2026 年 9 月 18 日完成针对“用户手动切换到其他程序后，模型可能把输入发到新前台程序”的修复。

核心变化：

- desktop_focus_window 现在会显式锁定桌面输入目标；
- 后续 desktop_screenshot 不再改变输入目标；
- 输入前核对 PID、进程创建时间、可执行文件路径以及 root-owner 窗口链；
- 目标程序自己的模态对话框可以继续接收输入；
- 同一进程中的另一个无关顶层窗口不会自动成为输入目标；
- 用户切换到其他应用后，截图仍可使用，但鼠标、滚轮和键盘输入会被拒绝，除非再次显式调用 desktop_focus_window。

开源工作树上的回归结果：

- Python 语法检查通过；
- 44 项测试通过；
- 另有 32 个参数子测试通过；
- 测试没有向日常应用发送真实键盘或鼠标输入。

## 0.1.2：登录后自动连接

2026 年 9 月 18 日验证了当前用户登录后自动连接机制。

计划任务使用普通用户权限启动，不在任务参数中保存 Tunnel ID 或 API key。监控脚本会核对 Tunnel 进程的 PID、启动时间和可执行文件路径。测试中人工终止 Tunnel 后，监控能够重新启动连接；通过正常停止流程创建手动停止标记后，监控不会立即重新拉起 Tunnel。

0.1.2 的 Python 回归测试结果为 38 项通过，另有 32 个参数子测试通过。

## 0.1.1：按键状态与输入保护

早期测试发现，用户操作鼠标或修饰键与自动化输入恰好重叠时，瞬时状态检查可能过于敏感。

0.1.1 改为：

- 最多等待 1 秒；
- 要求连续 100 毫秒没有修饰键或鼠标按钮处于按下状态；
- 等待期间继续检查暂停状态和焦点；
- 超时时报告具体按键或按钮；
- 不代表用户强制释放输入状态。

相关行为由模拟测试覆盖。

## 基础 MCP 与文件能力

已验证：

- MCP stdio 初始化和工具发现；
- 中文、emoji 的文本读取与写入；
- 覆盖文件前创建备份；
- 并发修改检测；
- 非法路径和受保护目录拒绝；
- 备用数据流、特殊文件属性和 DACL 相关保护；
- 暂停机制；
- 审计记录不包含文件正文；
- 显示器、窗口和截图元数据读取；
- Ctrl + Alt + F11 紧急暂停热键；
- PowerShell 脚本基础语法检查；
- UTF-8 中文文档读取。

## 测试边界

自动化测试中的键盘、鼠标、拖动、滚动和文字输入主要使用模拟对象，不会操作日常应用。真实桌面行为仍会受到 Windows 前台焦点、UIPI、锁屏、UAC 安全桌面、显示器配置和第三方软件影响。

回收站相关测试重点验证“失败时不降级为永久删除”，而不是批量执行真实删除。

凭据没有提交到仓库；.local、虚拟环境、备份、审计和 Tunnel 日志均被排除。

## 当前版本

源码版本：0.1.3

安全说明见 [SECURITY.zh-CN.md](SECURITY.zh-CN.md)。
