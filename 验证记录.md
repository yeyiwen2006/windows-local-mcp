# 本机验证记录

验证日期为 2026 年 9 月 17 日。

本机 MCP 服务已经实现并安装。用户随后完成账号配置，并报告 ChatGPT 桌面端的文件与桌面输入测试成功。本机隧道就绪检查返回 HTTP 200，审计记录也包含成功的截图、点击和文字输入调用。

## 已完成

- 0.1.1 版自动测试结果为 38 项通过，另有 32 个参数子测试通过。测试用时约 1.78 秒。
- 标准 MCP 客户端通过 stdio 完成初始化并发现 20 个工具。
- 经 MCP 创建中文文件、读取中文与 emoji、覆盖文件并取得旧内容备份。
- 自动测试验证覆盖前备份失败、并发修改、非法路径、备用数据流、受保护 DACL、服务目录保护、暂停与日志正文排除。
- 通过 MCP 读取真实显示器和窗口信息，并取得当前桌面的 PNG 图像。画面已人工查看，尺寸与缩放元数据一致。实际显示器分辨率为 3072×1920。
- 本机 Ctrl+Alt+F11 紧急暂停热键注册成功。
- 本机暂停和恢复脚本正常，且不会被调用者原有的状态目录环境变量影响。
- 所有 PowerShell 脚本语法检查通过，状态脚本在 Windows PowerShell 中运行正常。
- 依赖一致性检查通过。中文说明与源码按 UTF-8 读取，未发现替换字符或中文乱码。
- 使用独立子 Agent 审查桌面模块、文件模块、暂停机制和启动脚本；发现的阻断问题已修复。

## 测试边界

自动化测试中的键盘、鼠标、拖动、滚动和中文输入使用模拟对象，没有发送真实输入。用户随后通过 ChatGPT 完成真实记事本点击和中文输入测试。多显示器负坐标通过参数测试，本机当前只有一个显示器参与实测。

真实回收站删除未执行。测试验证回收失败时保留原文件，且没有永久删除的降级路径。

用户已在本机完成密钥配置、隧道认证和 ChatGPT 应用绑定，密钥未发到聊天中。缺少连接配置时，启动脚本会拒绝启动。官方隧道客户端版本为 v0.0.14，实际帮助命令与官方源码已经核对。

测试过程中曾遇到系统临时目录权限冲突，以及私有目录权限代码的 API 兼容错误。临时测试改在本任务的 work 目录运行，代码错误已修复后重新验证。

## 按键状态问题与修正

用户报告在未打开记事本时，模型尝试通过 Win+R 启动它，工具返回了修饰键或鼠标按钮仍按住的错误。旧代码使用瞬时检查，既不等待状态稳定，也不报告具体键名。旧审计没有记录按键名称，因此无法从历史记录确定当时是哪一个键或按钮触发。

0.1.1 改为最多等待 1 秒，且连续 100 毫秒没有修饰键或鼠标按钮按下才继续。等待期间保持暂停和焦点检查；超时会报告具体键名，不代替用户强行释放。新增模拟测试覆盖短暂按住后释放、持续按住时拒绝、暂停中断以及只检查当前按下状态的高位。

这次修正已通过自动测试，没有重新执行真实 Win+R 流程。若按键确实持续按住，服务仍会拒绝输入，应根据新错误中的名称排查。

## 0.1.2 登录后自动连接

2026 年 9 月 18 日为当前 Windows 用户安装了 `Windows Local MCP Tunnel` 计划任务。任务使用 Windows 自带的 PowerShell，以普通用户和隐藏窗口方式运行，不在任务参数中存放 Tunnel ID 或 API key。监控脚本每 15 秒核对经过 PID、启动时间和可执行文件路径验证的 Tunnel 进程。

实际验证中，先强制结束经过身份核对的 Tunnel 进程。监控脚本在下一次检查时启动了新的 Tunnel 进程，新的健康检查返回 HTTP 200。随后通过正常停止脚本创建手动停止标记，等待超过一个检查周期后进程仍保持停止；手动启动可以清除标记并恢复连接。重复执行安装脚本后，计划任务仍只有一个监控进程；再次结束 Tunnel 时，原监控进程保持运行并成功恢复连接。

0.1.2 重新安装到本机独立环境后，版本读取为 0.1.2。PowerShell 脚本语法检查通过；Python 回归测试使用项目外的独立临时目录，结果为 38 项通过，另有 32 个参数子测试通过。最终状态为未暂停，Tunnel 就绪检查返回 HTTP 200，计划任务处于运行状态。
